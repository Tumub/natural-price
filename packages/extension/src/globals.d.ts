/** Injected by build.mjs. */
declare const __SERVICE_URL__: string;
declare const __EXTRA_SERVER_SITES__: string[];
/** False in the published builds: the extension then has no server capability at all. */
declare const __SERVER_ENABLED__: boolean;
