# fetch-service

Clean-room fetch API. Loads one URL in a fresh Playwright context with no
cookies, a generic user agent and a chosen exit location, then returns the
extracted price.

Not started. See issues #5, #6, #9.
